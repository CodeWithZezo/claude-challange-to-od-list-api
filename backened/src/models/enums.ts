export enum Plan {
    FREE = "free",
    BASIC = "basic",
    PREMIUM = "premium",
    ENTERPRISE = "enterprise"
}

export enum Role {
    ADMIN = "admin",
    MEMBER = "member",
    MANAGER = "manager",
}